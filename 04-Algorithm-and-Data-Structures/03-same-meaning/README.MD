## Same Meaning (Palindrom)

```pseucode

STORE "str" with STRING value
STORE "unspace" with "str" value that SPLIT a string into an array of substrings then JOIN it again
STORE "reversed" with "unspace" value thath SPLIT a string into an array of substrings REVERSED them and JOIN it again

BOOLEAN IF "reversed" has the same value with "unspace" TRUE else FALSE

```